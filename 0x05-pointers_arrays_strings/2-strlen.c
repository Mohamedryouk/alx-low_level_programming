#include "main.h"
/**
 *_strlen- returns the length
 *@s: pointer from type int
 *Return: returns count
 */
int _strlen(char *s)
{
	int count;

	for (count = 0 ; *s != '\0' ; count++)
	return (count);
}
